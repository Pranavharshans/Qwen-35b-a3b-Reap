# Separate, opt-in donor target capture configuration.
# This does not alter the v0 causal configs or upgrade the observational label.
schema_version: 1
run_id: null
model:
  id: Qwen/Qwen3.5-35B-A3B
  revision: 59d61f3ce65a6d9863b86d2e96597125219dc754
  source_precision: bf16
  execution_precision: bf16
  text_only: true
runtime:
  seed: 20260903
  deterministic: true
  batch_size: 4
  max_input_tokens: 1024
  max_new_tokens: 0
  enable_thinking: false
  use_cache: false
  speculative_decoding: false
budget:
  # One PRO 6000 host-hour; the lead must replace the provider rate with the
  # live all-inclusive offer before any paid launch.
  max_gpu_hours: 8
  max_cost_usd: 10
  provider_rate_usd_per_hour: 1.60
  storage_limit_gb: 150
  deadline_utc: 2027-01-01T00:00:00Z
datasets:
  manifest: datasets/manifests/full-lengthmatched.jsonl
  split: full
intervention:
  mode: none
  manifest: null
  renormalize_router_weights: false
