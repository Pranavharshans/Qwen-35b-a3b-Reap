# Second FAU pass: teacher-forced selected-expert vector capture for later bridge work.
# Pin a new copy if the dataset, budget, deadline, or runtime settings change.
schema_version: 1
run_id: null
model:
  id: Qwen/Qwen3.8-Flash-Next
  revision: de4b8e4d43b917e7706784d8bb445c9af86a3540
  source_precision: bf16
  execution_precision: bf16
  text_only: true
runtime:
  seed: 20260903
  deterministic: true
  batch_size: 4
  max_input_tokens: 1024
  max_new_tokens: 0
  enable_thinking: false
  use_cache: false
  speculative_decoding: false
budget:
  max_gpu_hours: 24
  max_cost_usd: 1
  provider_rate_usd_per_hour: 0.01
  storage_limit_gb: 700
  deadline_utc: 2027-01-01T00:00:00Z
datasets:
  manifest: datasets/manifests/full.jsonl
  split: full
intervention:
  mode: none
  manifest: null
  renormalize_router_weights: false
