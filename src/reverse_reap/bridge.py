"""Public bridge-training API.

The implementation lives in :mod:`reverse_reap.bridge_training`; this short
module keeps the feature discoverable without coupling callers to the longer
internal filename.
"""

from reverse_reap.bridge_training import *  # noqa: F403
